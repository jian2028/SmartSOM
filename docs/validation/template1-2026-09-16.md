# Template 1 development verification — 2026-09-16

Source: local `main`, `eff3752` plus uncommitted changes. No commit or push.
This verifies implementation behavior, not research performance.

## Rule and physics gates

| Case | Required / qualified | Operations | All orders delivered by | Recorded endpoint |
| --- | --- | --- | --- | --- |
| Static | 12 / 12 | 36 | 288 | 288 |
| Arrival only | 16 / 16 | 48 | 403 | 2000 |
| Outage only | 12 / 12 | 36 | 288 | 2000 |
| Combined disturbance | 16 / 16 | 48 | 403 | 2000 |

All recordings pass the independent ledger and simulator execution audit.
The full outage `[20,40)` happens during machine 001's idle interval under this
policy; equal completion times do not prove processing pause behavior. The
separate in-process outage `[10,13)` checks that behavior: four productive ticks
from start 9 to finish 16, versus finish 13 without the outage.

Static hand-check samples (start → finish):

| Order / route | First operation | Second operation | Third operation |
| --- | --- | --- | --- |
| 01: 1→2→4 | 9→13 | 32→38 | 66→74 |
| 02: 2→1→3 | 25→29 | 42→48 | 63→71 |
| 03: 3→4→2 | 22→26 | 52→58 | 95→103 |
| 04: 4→3→1 | 48→52 | 73→79 | 109→117 |

Differences are exactly 4, 6 and 8 active ticks. Transport samples retain pickup,
drop, actual movement count and endpoint coordinates. Drop minus pickup is at
least movement count plus one interaction tick; pauses and congestion may add
waiting. Independent state checks reject teleportation, overlapping vehicles,
swaps, facility penetration, capacity overflow, missing/duplicate ownership,
skipped operations and duplicate deliveries. Three corrupted-record controls
(counter, early completion and teleportation) are rejected.

A transport hand check for order 01 starts at `(1,5)` and ends at `(2,2)`:
Manhattan distance `|2−1| + |2−5| = 4`; the recorded path takes four moves.
Pickup at tick 3 and drop at tick 8 differ by five: four moves plus the drop
interaction. The later `(9,2)→(8,9)` leg similarly takes eight moves plus one
interaction (56→65). Its congested `(3,2)→(8,2)` leg is different: seven actual
moves, one drop interaction and eight waits account for 15→31. We do not assume
that every routed leg attains Manhattan distance.

Static AGV movement counts are 234, 198, 185, 157; disturbed counts are 338, 252,
222, 196. All four vehicles participate. Counts are movements, not utilization.
The chosen zero-defect case does not establish quality-failure or battery behavior.

## Native replay

Both recordings were opened in actual macOS Qt windows using computer interaction.
Verified Play/Pause, forward and reverse step, seek and correct final delivery
counts. The disturbed view shows machine 001 DOWN at 20, UP at 40, and the first
new orders at 40. Final source recordings have identical states and events to
those inspected in the native windows.

The original speed combo popup caused an accessibility-stack SIGSEGV. A plain
Speed button now cycles the same seven rates. Native recheck verified the entire
cycle for static playback, and 2× playback/pause plus the correct 16-order end
state for disturbed playback. A focused UI test verifies timer intervals and
unchanged recording bytes. Initial crash evidence is retained, not concealed.
Screenshots are retained inline in the implementation task.

## Automated checks

- Existing physical runtime tests: 43 passed.
- Relevant configuration, learning, composition, example and workflow regression:
  152 passed, 24 warnings. The first sandbox attempt had 10 system-permission
  failures; the completed normal-host rerun is the valid result.
- Final demo and playback-focused tests: 10 passed, including seven demo tests and
  three playback tests. These overlap earlier checks; counts are not added.
- Model-bundle evaluation checks for both SPT and the new coordinated baseline:
  2 passed.
- Ruff lint and formatting pass; generated evidence remains ignored.

## Model status

The model is **not accepted**. The first trial resolved to
64×64 and 1,024 decisions due to experiment defaults; it was stopped and marked
invalid. Corrected recipes explicitly resolve to 128×128, at most 100,000
adapter decisions and 2,000 physical ticks per episode. No invalid trial is an
accepted model. The local training/tuning limit is six hours including that trial.

The corrected curriculum completed all three stages, each with 262,144 adapter
decisions and 256 updates (786,432 decisions total). All four role modules are
present, and checkpoint hashes confirm actual weight changes at every stage.
Weights pass from warmup to static to disturbed. Recorded cumulative stage-end
times are 356, 928 and 1,536 seconds; these include preceding development
evaluations. Fixed sample budgets finished before the six-hour upper limit;
the wall-clock limit was not exhausted.

All three endpoints scored zero deliveries and zero completed operations on
both development cases. The declared ranking therefore tied and selected the
earliest endpoint (warmup). Selection was frozen before final evaluation and
was not changed after seeing final results. Evaluation was restarted with
checkpoint-derived algorithm metadata; the immutable selection was preserved.

| Final case | Execution seeds | Qualified deliveries per seed | Accepted |
| --- | --- | --- | --- |
| Static | 501–505 | 0, 0, 0, 0, 0 of 12 | No |
| Disturbed | 501–505 | 0, 0, 0, 0, 0 of 16 | No |

All ten runs reached 2,000 ticks with stochastic inference and no rule takeover.
All ten independent ledgers reject incomplete delivery. Static replay audits
report a verified partial trajectory; disturbed audits pass replay consistency
because the configured horizon ended, which is explicitly not delivery success.
The evaluator exited 1 for failed acceptance, not an infrastructure exception.

The observed failure is absence of productive coordination, not missing model
files: development evaluations complete no operations; the last three warmup
training episodes spend about 70%, 85% and 84% of AGV choices waiting, with only
1, 0 and 1 machine-start choices respectively. These are proposed actions, not
proof of accepted starts. This suggests an exploration/coordination problem;
the evidence does not isolate a causal reward or network defect, and does not
establish that a larger training budget would fix it. No accepted-model command
is advertised. Failed checkpoints and all ten recordings remain available.

Final machine-readable model evidence:
`artifacts/template1/evaluation-20260916T040326Z/report.json`; frozen selection:
`artifacts/template1/curriculum-20260916T033525Z/selection.json`.

See `artifacts/template1/latest-validation.json`, `native-ui.json`, the independent
ledgers, source archives, regression logs and curriculum reports for evidence.
